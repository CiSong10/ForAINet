defaults: 
  - visualization: eval

num_workers: 0
batch_size: 1
cuda: 0
weight_name: "latest" # Used during resume, select with model to load from [miou, macc, acc..., latest]
enable_cudnn: True
# checkpoint_dir: "/home/cisong/ForAINet/outputs/pretrained"
checkpoint_dir: "/home/cisong/ForAINet/outputs/sanity/sanity-PointGroup-PAPER-20250818_135209"
model_name: PointGroup-PAPER
precompute_multi_scale: True # Compute multiscate features on cpu for faster training / inference
enable_dropout: False
voting_runs: 1
data: 
  # number, e.g. 3 OR ply path
  # test files for basic settings (remember change to your data path)
  fold: ['/home/cisong/ForAINet/data_set1_5classes/treeinsfused/raw/sanity/sanity_1_annotated_test.ply']
  # fold: ['/home/cisong/ForAINet/data_set1_5classes/treeinsfused/raw/NIBIO/NIBIO_plot_1_annotated_test.ply']
tracker_options: # Extra options for the tracker
  full_res: True
  make_submission: True
  ply_output: "vote1regular.ply"

hydra:
  run:
    dir: ${checkpoint_dir}/eval/${now:%Y-%m-%d_%H}


